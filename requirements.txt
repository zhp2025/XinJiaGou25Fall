# ========== 核心依赖（必需） ==========
Flask==3.0.0
Flask-Login==0.6.3
Werkzeug==3.0.1
python-dotenv==1.0.0
requests==2.31.0

# ========== AI 模型依赖（可选，根据使用的模型安装） ==========
# 阿里云通义千问（DashScope）
dashscope==1.17.0

# OpenAI（GPT-3.5, GPT-4）
openai==1.3.0

# Google Gemini
google-generativeai==0.3.1

# DeepSeek 和 Kimi 使用 requests 库（已包含在核心依赖中）

# ========== 数据库相关依赖（暂时不使用，后续接入数据库时再安装） ==========
# Flask-SQLAlchemy==3.1.1
# PyMySQL==1.1.0
# cryptography==41.0.7

